<?php

/**
 * Batch const class.
 * Batchで利用される定数
 */
class ConstBatch
{
  // 共通処理
  const LEN_TOKEN      = 32;
  const LEN_LOG_ID     = 20;
  const LEN_CLASS_NAME = 30;
  const LEN_FUNC_NAME  = 30;
  const LEN_LINE_NUM   = 5;
}
